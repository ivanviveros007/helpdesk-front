import { NewTicketScreen } from "@/screens/NewTicketScreen";

export const metadata = { title: "Nuevo ticket — Helpdesk AI" };

export default function NewTicketPage() {
  return <NewTicketScreen />;
}
